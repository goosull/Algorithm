#include <algorithm>
#include <iostream>
#include <vector>
using namespace std;

long long N, K;

int main() {
    cin >> N >> K;

    int temp;

    for (int i = 0; i < N; i++) {
        cin >> temp;
        cin >> temp;
    }

    cout << "비와이";

    return 0;
}